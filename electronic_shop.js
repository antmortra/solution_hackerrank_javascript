let b = 60;

const keyboard = [40, 50, 60];
const drives = [5, 8, 12];

const buy = [];

keyboard.forEach( el => {
    console.log(el);

    const tempt = [];

    drives.forEach( el2 => {
        console.log(el2)


    })
});